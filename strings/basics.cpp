#include<iostream>
using namespace std;
int main()
{
      // string s  = "UmarKhan";
      // int len = s.size();

      // cout<<len;  //this will print length 

      // s[len - 1] = 'O'; //here we can change value at last index or at any index
      // cout<<s[len - 1]; //as string are also index base and here last index will be print

      //while loop
      // int i = 1; // initialize
      // while(i <= 5) //check
      // {
      //       cout<<"Learning DSA "<< i << endl; // print 

      //        i = i + 1 ;  //increment
      // }

      //DO While LOOP
      int i = 1; // initialize
      do // Do it for one time rather condition true or false
      {
            cout<<"Lear to Lead "<<endl; //print

            i = i + 1; //increment

      } while (i < 0);  // check condition
      

      return 0;
}